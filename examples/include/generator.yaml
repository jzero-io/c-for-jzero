---
GENERATOR:
  PackageName: add
  PackageDescription: "Package add provides Go bindings for add.h"
  PackageLicense: "THE AUTOGENERATED LICENSE. ALL THE RIGHTS ARE RESERVED BY ROBOTS."
  Includes:
    - add.h
  FlagGroups:
    - {name: "CFLAGS", flags: [
      "-I../../include",
    ]}
    - { name: "LDFLAGS", flags: [
      "-L../../lib -ladd -Wl,-rpath,lib",
    ] }

PARSER:
  SourcesPaths:
    - add.h

TRANSLATOR:
  ConstRules:
    defines: expand
  Rules:
    global:
      - {action: accept, from: "^Add"}
